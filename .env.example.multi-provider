# Multi-LLM Provider Configuration
# Choose your preferred LLM provider and set API key accordingly.

# ============================================================================
# PROVIDER SELECTION (default: gemini)
# ============================================================================
LLM_PROVIDER=gemini
# Supported values: gemini, nvidia_nim

# ============================================================================
# GOOGLE GEMINI CONFIGURATION
# ============================================================================
# Use this when LLM_PROVIDER=gemini
# Get API key: https://aistudio.google.com/app/apikey
KIMI_API_KEY="AIza...your-gemini-key"

# Optional: Override Gemini model
# Default: gemini-1.5-pro (2M context)
# Alternatives: gemini-1.5-flash (1M context, faster, cheaper)
# LLM_MODEL=gemini-1.5-pro

# ============================================================================
# NVIDIA NIM CONFIGURATION
# ============================================================================
# Use this when LLM_PROVIDER=nvidia_nim
# Get API key: https://build.nvidia.com/
KIMI_API_KEY="nvapi-...your-nvidia-nim-key"

# Optional: Override NIM model (usually not needed)
# Default: moonshotai/kimi-k2.5 (256K context, reasoning-focused)
# LLM_MODEL=moonshotai/kimi-k2.5

# ============================================================================
# OPTIONAL: Debugging and Performance
# ============================================================================
SWARM_DEBUG=0                       # Enable debug logging
SWARM_API_TIMEOUT=120               # API timeout in seconds
SWARM_API_RATE_LIMIT=10             # Requests per minute
SWARM_WORKSPACE_ROOT=~/.openclaw/workspace/android-swarm

# ============================================================================
# QUICK START
# ============================================================================
# 1. Copy this file: cp .env.example.multi-provider .env
# 2. Set your API key (Gemini or NVIDIA NIM)
# 3. Choose provider (default is Gemini)
# 4. Run: node dist/index.js agent --message '...'
#
# To switch providers:
# - Edit .env and change LLM_PROVIDER
# - OR set env var: export LLM_PROVIDER=nvidia_nim
# - Then run the same command
#
# WARNING: Never commit .env with real API keys!
